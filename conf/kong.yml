_format_version: "3.0"
_transform: true

services:
  - name: PECA
    protocol: http
    host: localhost
    port: 3000
    path: /api/v1/peca
    routes:
      - name: Pecas
        methods:
          - GET
            POST
            PATCH
            DELETE
        paths:
          - /v1/gateway/peca
        strip_path: true
